# AGENTS.md

## Learned User Preferences

- Be skeptical and accurate over polite (steel-man a questionable request in one sentence, then say why it still fails; don't argue when the writing or code is already fine); never invent or embellish project/resume details, and ship placeholders rather than fabricated content.
- The user works in small, independently-scoped chunks: when a change gets involved, they like it broken into small, self-contained actions/tickets they can explore one at a time and hand off to (or flag for) their team, so proactively offer that scoped breakdown rather than one monolithic change. They also gate shipping on their own review: do not deploy/host/publish until they have reviewed and approved (e.g. "no shipping until I review everything"; prefer running the site locally for them to look first).
- Do not use em-dashes or " -- " in prose written for the user; on the site, page/OG titles use the middot form ("Title · Niran D.") and date ranges use spaceless en dashes (e.g. "2022–Present", "2020–2021").
- Position the user as "AI-adjacent," never "AI-focused" (a deliberate choice to reduce interview risk); more broadly, for job-search copy the user wants to read as a versatile senior generalist, not a niche/field-specific expert, even if that means generalizing away from an over-used theme word (e.g. dropping a "trust"-heavy framing). Depth and specifics belong in the case studies, not the hero.
- Do not raise case-study attribution concerns for the user's projects (e.g. KAYAK, PartyPoint); the user has confirmed these are work they did.
- Portfolio font system (current): Circular Std (self-hosted display/headings via --display) + Source Sans 3 (body via --sans, Google Fonts). Palette: bg #ffffff, ink rgba(0,0,0,.88), slate-blue accent #2f4a6b. Keep layouts clean and easy to read, with a left-aligned home hero and generous spacing on case studies.
- Wants tasteful delight/microinteractions on the portfolio (Web Audio click sounds, now always on with the header on/off toggle removed; a cursor "View Project (+)" label, magnetic CTAs, copy-email confirmation, avatar jump), but considers heavy effects (particle/comet cursor trails, image distortion, full-page parallax) too much for a hiring portfolio.
- When polishing portfolio copy, keep a credible senior product designer voice (clear, strategic, framed around problem, trade-offs, and impact), never salesy, dramatic, or influencer-y. Reduce AI-sounding language: avoid buzzwords (leveraged, seamless, delightful, innovative, robust, user-centric), "thesis/hook" style headlines, and "not X, but Y" antithesis/aphoristic openers, unless truly needed. Do not over-edit: only rewrite sections that need it and leave already-strong prose intact.
- Keep portfolio motion clean and restrained: the user explicitly rejects image zoom/Ken-Burns effects and shared-element image-morph transitions between the home and case-study pages as distractions. The motion they want is scroll-triggered text-highlight reveals (the highlight sweeps across the text left-to-right, firing when scrolled into view rather than on page load) and rolling-number counters for case-study stats. All motion is gated behind prefers-reduced-motion, and the Web Audio click sound is always on (the header on/off toggle was removed).
- Personal/side-project sections (e.g. the Photos section and the Zoom presentation-engagement app case study) should read as intentional product thinking that conveys personality (explores outside work, solves problems, brings joy), while staying restrained enough for a hiring portfolio.
- For the Photos section: avoid a plain gallery/grid and a map (the Instagram export's GPS is unreliable); organize by visual "modes of seeing," and do not show dates or a timeline (the user hasn't shot recently and does not want to signal recency).
- When the user shares a reference site they like (e.g. andreavollendorf.com for the home page, roupenjam.com for hero-copy structure), replicate its layout, typography, microinteractions, and even sentence structure (e.g. "[Role]. I [do X], [do Y], and [do Z].") faithfully rather than approximating, but never copy the literal wording; they push back hard when the result diverges from the reference. Case studies should be story-driven and visually compelling (a long page is fine) with interactive elements where they help, not walls of text, and outcomes stay honest and qualitative (no fabricated metrics). Present case-study pages like a Medium article scannable at a glance for a hiring manager: large, readable body text and large images (the user repeatedly flags tiny text/images as the main problem), key numbers and takeaways highlighted, and any flowchart recreated as a native on-page visual (custom UI/animation via the canvas/frontend skills) rather than embedded as a flat image; check accessibility, and cut redundant content before a first review.

## Learned Workspace Facts

- The portfolio project lives at /Users/ndonthireddy/Desktop/Portfolio (index.html, niran-greeter.html, img/), not in Downloads.
- The portfolio is a Vite + React multi-page static site (npm run dev / npm run build / npm run preview): hand-authored HTML pages are Vite entries via vite.config.js, sharing site.css and public/site.js with a common top nav (Work dropdown menu uses 8px border-radius). Deployed to GitHub Pages via GitHub Actions (.github/workflows/deploy.yml) on push to main, outputting dist/ to custom domain www.thisisgreat.design (public/CNAME); repo is git-initialized locally. Dev-only tooling: localhost-only layout grid overlay toggled with G in public/site.js; interface-kit as a dev-only React island on index via src/dev.jsx (tree-shaken from production, must never ship). Individual pages still carry some page-specific inline CSS/JS. Must stay responsive on mobile (0px horizontal overflow across pages).
- The owner is Niran D., Senior Product Designer (7+ years); current role at KAYAK since 2022, prior roles at HighRadius, Ivy/PartyPoker, MAQ Software, and HelloFello.
- Case study sources: the KAYAK "AI Chat" and "Search overview" studies come from xtai.me (xtai.me/design/kayak-ai-chat, xtai.me/design/search-overview); the HighRadius "Autonomous Receivables" study comes from ravinchoudhary11.wixsite.com/website/autonomous-collections (the user was its lead designer) and should become a dedicated case-study page, opened from the HighRadius item and tagged "finance." The user wants the videos from these pages included and autoplaying, not skipped. The kayak-ai-chat.html study is structured as the "AI Ghost Town" narrative: standalone KAYAK.ai (launched 2024) drew almost no return visits despite 5x referral CTR, motivating embedding AI into KAYAK.com; NLP Search ships as the cheapest MVP test; a prompt-engineering section contrasts a discarded parametric classifier (Framework A) with the adopted intent-inference engine (Framework B, recreated as a native on-page branch flowchart); an "At a glance" summary opens the page (onboarding to embedded drawer, multi-intent flight+hotel widgets, responsive chat). Cover video is img/kayak-aichat-cover.mp4 and source assets came from ~/Downloads/chat ai assets (named for placement).
- Animations use Motion One (via cdnjs). Case studies are dedicated pages with side navigation (replacing the earlier in-page project modal); the image/photo lightbox modal still has a focus trap, Escape/backdrop-click close, and focus return to the trigger.
- Project images are wired via a CASE_IMAGES array and a per-entry hero field in CASE_DATA.
- niran.png is a 768x640 sprite sheet (6 columns x 5 rows, 128px frames): row 0 = idle, row 1 = walk, row 2 = jump, row 4 = wave; render pixelated with white pixels kept opaque. The hero avatar greeter that used it (idle plus a row-2 jump inside the profile circle, with the wave and the full-page grass/sky/mountains background both dropped) was later removed from the home page and now survives only in the deprecated, unlinked niran-greeter.html demo.
- The user has a local Instagram export on the Desktop (an instagram-xxnran-... folder): media sits at the export root under media/ (posts/ = 75 curated images spanning Mar 2022 to May 2024; stories/ = 236 ephemeral, excluded); post metadata is HTML (your_instagram_activity/content/posts_1.html), not JSON; GPS is "posted-from" and unreliable; the export also contains DMs (never surface).
- The home page (index.html) is a centered ~880px editorial column (originally modeled on andreavollendorf.com, hero-copy structure later adapted from roupenjam.com): a left-aligned hero with one plain declarative h1 ("Product designer. I [do X], [do Y], and [do Z]."; no "Senior," no separate thesis/subhead paragraphs), a "Currently at" caption using an inline SVG KAYAK full wordmark with a per-tile staggered wave hover animation, linked out. No hero button row (email/LinkedIn/Resume live in the nav Contact dropdown and Contact section). Below that: "Select work" (hairline divider above the heading and a one-line subheading) listing case-study cards whose autoplay video/media now fills the frame full-bleed (`.cs-frame` padding 0, `.cs-media` border-radius 0, `article.cs` 16px padding, the frame keeping a 16px radius + overflow hidden); earlier experiments with loud/dark abstract image card backgrounds and then subtle gray gradient card backgrounds were both dropped once the media went full-bleed (the shared `.wordmark` nav size was bumped to 1.125rem in the same pass), a 2-up "Misc. Work" gradient-tile grid that opens the impact modal, and a "Contact" section.
- The "Come touch grass" Zoom-presentation engagement project is now a dedicated interactive case study (come-touch-grass.html), linked as a teaser from play.html. Its source lives at ~/Desktop/AI Presentation _ design weekly/ (a PartyKit/WebSocket back-channel where audience members join from phones, pick a pixel avatar, and appear live in a shared pixel meadow; also the origin of the niran.png sprite). The page vendors a trimmed sprite engine (play-stage.js) using the project's team PNG sheets in img/crowd/ (rows: idle/walk/cheer/clap/wave), a playable canvas stage with a "Control the room" panel (wave/cheer/clap/raise-hand/emotes/say/summon-mascot), a hero dead-Zoom-grid to living-meadow transform, and IntersectionObserver scrollytelling. Keep it anonymized: generic handle names, the dog renamed from "Arlo," no employer/team names, no internal tools (e.g. Glean/Atlassian), and qualitative outcomes only.
- The Photos section (photos.html) is generated by build_photos.py from 63 curated, web-optimized webp images in img/photos/, grouped into five "modes of seeing" (Wide, Last Light, Built, Up Close, Still) after cutting the weak market/street and dark frames. The build conservatively auto-trims baked-in near-white letterbox borders and deterministically shuffles (fixed seed) the frames within each mode so they read like a real contact sheet rather than sorted location folders. Its treatment is a restrained "contact sheet / proof sheet" (film plus paper-print metaphor): off-white photo paper, film rebate edges, sprocket holes, frame numbers, a ~5-column 3:2 grid, and a print-style lightbox showing the full uncropped frame with a placard (the earlier grease-pencil "select" red circle was removed at the user's request). Instagram strips camera/lens/exposure/GPS/date EXIF on export, so only captions survive; they are used verbatim as location/subject placards in the lightbox (no dates, no invented places).
- The Process page (process.html) is a "technical field-guide" inspired by makingsoftware.com: the five stages (Frame, Validate, Prototype, Systemize, Iterate) are numbered, annotated inline-SVG schematics (Fig. 00 to 05) on subtle graph-paper frames with bracketed figure captions, leader lines, crop marks, and parameter readouts, drawing themselves in on scroll. Per the user, the illustrations stay ink-only line-art with a single slate accent (#2f4a6b), sized to match the body text (no oversized figures, no overlapping elements), and use letter-spaced uppercase caps instead of monospace (keeping the site's current display/body font pairing).
